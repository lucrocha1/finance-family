import { FileDown } from "lucide-react";

import { PagePlaceholder } from "@/components/layout/PagePlaceholder";

const ImportCsvPage = () => {
  return <PagePlaceholder icon={FileDown} title="Importar CSV" />;
};

export default ImportCsvPage;
