import { CalendarDays } from "lucide-react";

import { PagePlaceholder } from "@/components/layout/PagePlaceholder";

const SchedulePage = () => {
  return <PagePlaceholder icon={CalendarDays} title="Agenda" />;
};

export default SchedulePage;
