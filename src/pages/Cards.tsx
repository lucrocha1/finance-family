import { CreditCard } from "lucide-react";

import { PagePlaceholder } from "@/components/layout/PagePlaceholder";

const CardsPage = () => {
  return <PagePlaceholder icon={CreditCard} title="Cartões" />;
};

export default CardsPage;
