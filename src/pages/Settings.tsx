import { Settings } from "lucide-react";

import { PagePlaceholder } from "@/components/layout/PagePlaceholder";

const SettingsPage = () => {
  return <PagePlaceholder icon={Settings} title="Configurações" />;
};

export default SettingsPage;
